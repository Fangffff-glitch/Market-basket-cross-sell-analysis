# Data

Raw course-provided datasets are not included in this repository.

To run the notebook locally, add compatible CSV files to:

```text
data/csv/
```

Expected files:

- `transaction_data.csv`
- `product.csv`
- `causal_data.csv`

